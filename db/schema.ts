import {sql} from 'drizzle-orm';
import {sqliteTable,text,integer,index,uniqueIndex} from 'drizzle-orm/sqlite-core';
export const members=sqliteTable('members',{id:text('id').primaryKey(),email:text('email').notNull(),name:text('name').notNull(),role:text('role').notNull(),createdAt:text('created_at').notNull()},t=>[uniqueIndex('members_email').on(t.email)]);
export const settings=sqliteTable('settings',{key:text('key').primaryKey(),data:text('data').notNull()});
export const programs=sqliteTable('programs',{id:text('id').primaryKey(),data:text('data').notNull(),revision:integer('revision').notNull().default(1)});
export const modules=sqliteTable('modules',{id:text('id').primaryKey(),programId:text('program_id').notNull().references(()=>programs.id),data:text('data').notNull()});
export const calls=sqliteTable('calls',{id:text('id').primaryKey(),programId:text('program_id').notNull().references(()=>programs.id),moduleId:text('module_id').references(()=>modules.id),data:text('data').notNull()});
export const variants=sqliteTable('variants',{id:text('id').primaryKey(),programId:text('program_id').notNull().references(()=>programs.id),callId:text('call_id').references(()=>calls.id),data:text('data').notNull()});
export const sources=sqliteTable('sources',{id:text('id').primaryKey(),programId:text('program_id'),url:text('url').notNull(),data:text('data').notNull(),lastAttempt:text('last_attempt'),lastFetch:text('last_fetch'),lastCheck:text('last_check'),error:text('error')},t=>[uniqueIndex('sources_url').on(t.url)]);
export const versions=sqliteTable('versions',{id:text('id').primaryKey(),documentId:text('document_id').notNull(),sourceId:text('source_id').notNull().references(()=>sources.id),programId:text('program_id').notNull().references(()=>programs.id),hash:text('hash').notNull(),current:integer('current').notNull().default(1),data:text('data').notNull()},t=>[uniqueIndex('version_fingerprint').on(t.documentId,t.hash),index('versions_program').on(t.programId)]);
export const claims=sqliteTable('claims',{id:text('id').primaryKey(),programId:text('program_id').notNull().references(()=>programs.id),field:text('field').notNull(),active:integer('active').notNull().default(1),data:text('data').notNull()},t=>[index('claims_program_field').on(t.programId,t.field)]);
export const evidence=sqliteTable('evidence',{id:text('id').primaryKey(),claimId:text('claim_id').notNull().references(()=>claims.id),versionId:text('version_id').notNull().references(()=>versions.id),data:text('data').notNull()},t=>[index('evidence_version').on(t.versionId)]);
export const reviews=sqliteTable('reviews',{id:text('id').primaryKey(),claimId:text('claim_id').notNull(),actor:text('actor').notNull(),at:text('at').notNull(),action:text('action').notNull(),reason:text('reason').notNull(),data:text('data').notNull()});
export const questions=sqliteTable('questions',{id:text('id').primaryKey(),programId:text('program_id').notNull(),data:text('data').notNull()});
export const events=sqliteTable('events',{id:text('id').primaryKey(),programId:text('program_id'),kind:text('kind').notNull(),at:text('at').notNull(),data:text('data').notNull()},t=>[index('events_at').on(t.at)]);
export const runs=sqliteTable('runs',{id:text('id').primaryKey(),dayKey:text('day_key'),startedAt:text('started_at').notNull(),endedAt:text('ended_at'),status:text('status').notNull(),data:text('data').notNull()},t=>[uniqueIndex('runs_day').on(t.dayKey),uniqueIndex('runs_single_active').on(sql`1`).where(sql`${t.status} IN ('geplant','läuft')`)]);
export const jobs=sqliteTable('jobs',{id:text('id').primaryKey(),runId:text('run_id').notNull().references(()=>runs.id),kind:text('kind').notNull(),status:text('status').notNull(),attempts:integer('attempts').notNull().default(0),availableAt:text('available_at').notNull(),leaseUntil:text('lease_until'),leaseToken:text('lease_token'),data:text('data').notNull(),error:text('error')},t=>[index('jobs_ready').on(t.status,t.availableAt),index('jobs_run').on(t.runId)]);
export const searches=sqliteTable('searches',{id:text('id').primaryKey(),runId:text('run_id').notNull(),region:text('region').notNull(),theme:text('theme').notNull(),query:text('query').notNull(),status:text('status').notNull(),at:text('at'),data:text('data').notNull()});
export const candidates=sqliteTable('candidates',{id:text('id').primaryKey(),url:text('url').notNull(),status:text('status').notNull(),data:text('data').notNull()},t=>[uniqueIndex('candidate_url').on(t.url)]);
export const projects=sqliteTable('projects',{id:text('id').primaryKey(),ownerId:text('owner_id').notNull(),programId:text('program_id').notNull(),data:text('data').notNull()},t=>[index('projects_owner').on(t.ownerId)]);
export const uploads=sqliteTable('uploads',{id:text('id').primaryKey(),projectId:text('project_id').notNull().references(()=>projects.id),ownerId:text('owner_id').notNull(),objectKey:text('object_key').notNull(),data:text('data').notNull()});
export const chats=sqliteTable('chats',{id:text('id').primaryKey(),ownerId:text('owner_id').notNull(),programId:text('program_id').notNull(),at:text('at').notNull(),revision:integer('revision').notNull(),data:text('data').notNull()},t=>[index('chats_owner_program').on(t.ownerId,t.programId)]);
export const correspondence=sqliteTable('correspondence',{id:text('id').primaryKey(),ownerId:text('owner_id').notNull(),projectId:text('project_id').notNull(),data:text('data').notNull()});
